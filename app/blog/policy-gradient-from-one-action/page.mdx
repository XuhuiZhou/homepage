import {
  OneStepTrajectory,
  ReturnDecomposition,
  QExpectationDemo,
  AdvantageBaselineDemo,
  GradientLadder,
} from './ChartComponents'

export const metadata = {
  title: 'Policy Gradient From One Answer',
  description:
    'A visual derivation of policy gradient for language models, carefully walking from terminal reward to return, Q-value, and advantage.',
  date: '2026-07-17',
  authors: [{ name: 'Xuhui Zhou' }],
  openGraph: {
    title: 'Policy Gradient From One Answer',
    description:
      'The exact math behind the policy-gradient ladder: R to G_t, G_t to Q, and Q to advantage.',
    type: 'article',
    publishedTime: '2026-07-17',
    authors: ['Xuhui Zhou'],
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Policy Gradient From One Answer',
    description:
      'A visual derivation of policy gradient from terminal reward to return, Q, and advantage.',
    creator: '@nlpxuhui',
  },
}

# Policy Gradient From One Answer

<div className="not-prose mb-8 border-b border-zinc-200 pb-6">
  <div className="text-sm text-zinc-500">By Xuhui Zhou · Jul 17, 2026</div>
  <div className="mt-1 text-sm text-zinc-400 italic">
    The math ladder from reward to return to Q to advantage.
  </div>
</div>

The core policy-gradient move is simple: if an action leads to better future outcomes than expected,
make that action more likely next time. The hard part is being precise about what "future outcomes"
means.

This post is about one chain of equalities:

$$
R
\quad\Longrightarrow\quad
G_t
\quad\Longrightarrow\quad
Q^\pi(s_t,a_t)
\quad\Longrightarrow\quad
A^\pi(s_t,a_t).
$$

Each arrow is not a vibe. Each arrow is a legal replacement inside an expectation.

## The one-answer case

Start with the smallest language-model RL problem:

<Figure id="one-step">
  <OneStepTrajectory />
</Figure>

A prompt $x$ arrives. The model samples one complete answer $y \sim \pi_\theta(\cdot \mid x)$.
A judge gives one terminal reward $R(x,y)$. If the full answer is treated as one action, the objective is:

$$
J(\theta \mid x)
=
\mathbb{E}_{y \sim \pi_\theta(\cdot \mid x)}
\left[R(x,y)\right].
$$

Write the expectation as a sum:

$$
J(\theta \mid x)
=
\sum_y
\pi_\theta(y \mid x)R(x,y).
$$

Differentiate it:

$$
\begin{aligned}
\nabla_\theta J(\theta \mid x)
&=
\sum_y
\nabla_\theta \pi_\theta(y \mid x)R(x,y) \\
&=
\sum_y
\pi_\theta(y \mid x)
\nabla_\theta \log \pi_\theta(y \mid x)
R(x,y) \\
&=
\mathbb{E}_{y \sim \pi_\theta(\cdot \mid x)}
\left[
R(x,y)\nabla_\theta \log \pi_\theta(y \mid x)
\right].
\end{aligned}
$$

The tiny trick is:

$$
\nabla_\theta \pi_\theta(y \mid x)
=
\pi_\theta(y \mid x)
\nabla_\theta \log \pi_\theta(y \mid x).
$$

Intuition: we are not differentiating the judge. We are differentiating how likely the model was to
produce the sampled answer. High reward says "increase this answer's log-probability." Later, after
we subtract a baseline, worse-than-expected reward says "decrease it."

This one-answer case is the whole story when $T=1$. Now we make $T$ bigger.

## The trajectory case

A real trajectory has many actions:

$$
\tau=(s_1,a_1,r_1,s_2,a_2,r_2,\ldots,s_T,a_T,r_T).
$$

For now, use the undiscounted return:

$$
G_1
=
\sum_{k=1}^{T} r_k.
$$

The objective is:

$$
J(\theta)
=
\mathbb{E}_{\tau \sim p_\theta}
\left[G_1\right].
$$

The trajectory probability factors as:

$$
p_\theta(\tau)
=
\rho(s_1)
\prod_{t=1}^{T}
\pi_\theta(a_t \mid s_t)
P(s_{t+1}\mid s_t,a_t).
$$

Only the policy terms contain $\theta$. The initial-state distribution $\rho$ and environment
transition $P$ do not. So:

$$
\begin{aligned}
\nabla_\theta \log p_\theta(\tau)
&=
\nabla_\theta
\left[
\log \rho(s_1)
+
\sum_{t=1}^{T}\log \pi_\theta(a_t\mid s_t)
+
\sum_{t=1}^{T}\log P(s_{t+1}\mid s_t,a_t)
\right] \\
&=
\sum_{t=1}^{T}
\nabla_\theta \log \pi_\theta(a_t\mid s_t).
\end{aligned}
$$

Now use the same log-derivative trick as before:

$$
\begin{aligned}
\nabla_\theta J(\theta)
&=
\nabla_\theta
\int p_\theta(\tau)G_1\,d\tau \\
&=
\int
p_\theta(\tau)
\nabla_\theta \log p_\theta(\tau)
G_1\,d\tau \\
&=
\mathbb{E}_{\tau \sim p_\theta}
\left[
G_1
\sum_{t=1}^{T}
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right] \\
&=
\mathbb{E}_{\tau \sim p_\theta}
\left[
\sum_{t=1}^{T}
G_1
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right].
\end{aligned}
$$

This is the first raw trajectory formula. It says: attach the total return $G_1$ to every action.

But this is wasteful. Action $a_3$ should not get credit for reward $r_1$, because $r_1$ already
happened before $a_3$ was chosen. That is where reward-to-go enters.

## From total return to reward-to-go

Define the reward-to-go from time $t$:

$$
G_t
=
\sum_{k=t}^{T} r_k.
$$

So:

$$
G_1
=
\underbrace{\sum_{k=1}^{t-1} r_k}_{\text{past before }a_t}
+
\underbrace{\sum_{k=t}^{T} r_k}_{G_t}.
$$

For the gradient term at time $t$:

$$
\mathbb{E}
\left[
G_1
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right]
=
\mathbb{E}
\left[
G_t
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right]
$$

because the past part has zero expectation when multiplied by the score function.

Here is the conditional proof. Let $H_t=(s_1,a_1,r_1,\ldots,s_t)$ be the history before sampling
$a_t$. The past reward prefix is already fixed once $H_t$ is fixed:

$$
\text{Past}_t
=
\sum_{k=1}^{t-1} r_k.
$$

Then:

$$
\begin{aligned}
\mathbb{E}
\left[
\text{Past}_t
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\mid H_t
\right]
&=
\text{Past}_t
\sum_{a}
\pi_\theta(a\mid s_t)
\nabla_\theta \log \pi_\theta(a\mid s_t) \\
&=
\text{Past}_t
\sum_{a}
\nabla_\theta \pi_\theta(a\mid s_t) \\
&=
\text{Past}_t
\nabla_\theta
\sum_a
\pi_\theta(a\mid s_t) \\
&=
\text{Past}_t
\nabla_\theta 1 \\
&=0.
\end{aligned}
$$

That is the whole reason we can replace $G_1$ by $G_t$ in the $t$-th term:

$$
\begin{aligned}
\nabla_\theta J(\theta)
&=
\mathbb{E}\left[
\sum_{t=1}^{T}
G_t
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right].
\end{aligned}
$$

<Figure id="return-decomposition">
  <ReturnDecomposition />
</Figure>

Intuition: action $a_t$ cannot reach backward in time and change rewards that already happened.
Those past rewards are like a constant baseline for this action. Score functions erase constants.

If using discounting, define:

$$
G_t
=
\sum_{k=t}^{T}
\gamma^{k-t}r_k.
$$

For the objective $J=\mathbb{E}[G_1]$, the exact derivation gives a factor
$\gamma^{t-1}G_t$ in front of the $t$-th score term. Many presentations either include that factor
or absorb it into how timesteps are weighted. The important idea is unchanged: past rewards vanish;
future rewards stay.

## From sampled return to Q

Reward-to-go $G_t$ is still a sampled future. If you take the same state $s_t$ and same action
$a_t$, many different futures can happen.

The action-value function averages those futures:

$$
Q^\pi(s_t,a_t)
=
\mathbb{E}_\pi
\left[
G_t
\mid
s_t,a_t
\right].
$$

Now the question is: why can $G_t$ be replaced by $Q^\pi(s_t,a_t)$ in the gradient?

Let:

$$
Z_t
=
\nabla_\theta \log \pi_\theta(a_t\mid s_t).
$$

The key is the tower property of expectation:

$$
\begin{aligned}
\mathbb{E}
\left[
Z_tG_t
\right]
&=
\mathbb{E}
\left[
\mathbb{E}
\left[
Z_tG_t
\mid
s_t,a_t
\right]
\right] \\
&=
\mathbb{E}
\left[
Z_t
\mathbb{E}
\left[
G_t
\mid
s_t,a_t
\right]
\right] \\
&=
\mathbb{E}
\left[
Z_t
Q^\pi(s_t,a_t)
\right].
\end{aligned}
$$

The second line is legal because $Z_t$ is fully determined once $(s_t,a_t)$ is known. It can be
pulled outside the inner expectation.

So:

$$
\begin{aligned}
\nabla_\theta J(\theta)
&=
\mathbb{E}\left[
\sum_{t=1}^{T}
Q^\pi(s_t,a_t)
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right].
\end{aligned}
$$

<Figure id="q-expectation">
  <QExpectationDemo />
</Figure>

Intuition: $G_t$ is one roll of the future dice. $Q^\pi(s_t,a_t)$ is the average of many such rolls.
They have the same expectation in the gradient, but $Q$ is less noisy if you can estimate it well.

## From Q to advantage

Now we do one more legal replacement.

The value function is the policy's average action-value at state $s$:

$$
V^\pi(s)
=
\mathbb{E}_{a\sim\pi(\cdot\mid s)}
\left[
Q^\pi(s,a)
\right].
$$

The advantage is:

$$
A^\pi(s,a)
=
Q^\pi(s,a)-V^\pi(s).
$$

Why can $Q$ be replaced by $A$? Because $V^\pi(s_t)$ is a baseline that depends on the state but not
on the sampled action. For any fixed state $s$:

$$
\begin{aligned}
\mathbb{E}_{a\sim\pi(\cdot\mid s)}
\left[
V^\pi(s)
\nabla_\theta \log \pi_\theta(a\mid s)
\right]
&=
V^\pi(s)
\sum_a
\pi_\theta(a\mid s)
\nabla_\theta \log \pi_\theta(a\mid s) \\
&=
V^\pi(s)
\sum_a
\nabla_\theta \pi_\theta(a\mid s) \\
&=
V^\pi(s)
\nabla_\theta 1 \\
&=0.
\end{aligned}
$$

Therefore:

$$
\begin{aligned}
\mathbb{E}
\left[
Q^\pi(s_t,a_t)
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right]
&=
\mathbb{E}
\left[
\bigl(Q^\pi(s_t,a_t)-V^\pi(s_t)\bigr)
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right] \\
&=
\mathbb{E}
\left[
A^\pi(s_t,a_t)
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right].
\end{aligned}
$$

So the policy-gradient theorem becomes:

$$
\begin{aligned}
\nabla_\theta J(\theta)
&=
\mathbb{E}_{\tau\sim\pi_\theta}\left[
\sum_{t=1}^{T}
A^\pi(s_t,a_t)
\nabla_\theta \log \pi_\theta(a_t\mid s_t)
\right].
\end{aligned}
$$

<Figure id="advantage-baseline">
  <AdvantageBaselineDemo />
</Figure>

Intuition: $Q$ asks, "How good is this action?" Advantage asks, "How good is this action compared
with what the policy usually does at this exact state?" That comparison is what gives the clean
push-up or push-down signal.

## Back to LLMs with terminal reward

For an autoregressive language model, the trajectory is a sequence of token actions:

$$
y=(y_1,\ldots,y_T),
\quad
\pi_\theta(y\mid x)
=
\prod_{t=1}^{T}
\pi_\theta(y_t\mid x,y_{\lt t}).
$$

If there is only one reward at the end, set:

$$
r_1=\cdots=r_{T-1}=0,
\quad
r_T=R(x,y).
$$

Then, without discounting:

$$
G_t
=
R(x,y)
\quad
\text{for every token }t.
$$

This is the exact reason terminal-reward LLM RL broadcasts one final score across all sampled
tokens:

$$
\nabla_\theta J(\theta)
\approx
R(x,y)
\sum_{t=1}^{T}
\nabla_\theta
\log
\pi_\theta(y_t\mid x,y_{\lt t}).
$$

If we train a critic, use rollouts, or use a group baseline, we are trying to replace that raw
terminal $R$ with a better estimate of advantage:

$$
\nabla_\theta J(\theta)
\approx
\sum_{t=1}^{T}
\widehat{A}_t
\nabla_\theta
\log
\pi_\theta(y_t\mid x,y_{\lt t}).
$$

The policy-gradient machinery did not change. Only the multiplier attached to each token became
more informative:

<Figure id="gradient-ladder">
  <GradientLadder />
</Figure>

## The whole derivation in one line

The mathematically careful story is:

$$
\begin{aligned}
\nabla_\theta J(\theta)
&=
\mathbb{E}
\left[
\sum_t
G_1
\nabla_\theta\log\pi_\theta(a_t\mid s_t)
\right] \\
&=
\mathbb{E}
\left[
\sum_t
G_t
\nabla_\theta\log\pi_\theta(a_t\mid s_t)
\right] \\
&=
\mathbb{E}
\left[
\sum_t
Q^\pi(s_t,a_t)
\nabla_\theta\log\pi_\theta(a_t\mid s_t)
\right] \\
&=
\mathbb{E}
\left[
\sum_t
A^\pi(s_t,a_t)
\nabla_\theta\log\pi_\theta(a_t\mid s_t)
\right].
\end{aligned}
$$

Each line is the same gradient in expectation:

- $G_1 \to G_t$: remove past rewards; they are action-independent baselines.
- $G_t \to Q^\pi$: average over possible futures using conditional expectation.
- $Q^\pi \to A^\pi$: subtract the state value baseline; it has zero expected score-function term.

That is the math behind policy gradient: do not backprop through the reward. Backprop through the
log-probability of the sampled action, weighted by how much better its future was than expected.
